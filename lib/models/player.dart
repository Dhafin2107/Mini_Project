class Employee {
  String id, name, position, imageUrl;
  DateTime createdAt;

  Employee({
    required this.position,
    required this.id,
    required this.imageUrl,
    required this.name,
    required this.createdAt});
}
